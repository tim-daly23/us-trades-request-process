# Supabase — from Project Settings > API
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server only. Bypasses RLS entirely — never expose to the browser,
# never prefix with NEXT_PUBLIC_.
SUPABASE_SERVICE_ROLE_KEY=

# TempWorks
TEMPWORKS_CLIENT_ID=
TEMPWORKS_CLIENT_SECRET=
TEMPWORKS_BASE_URL=

# Server only. Bypasses RLS entirely — required to create portal logins
# (Supabase Auth Admin API). Never prefix with NEXT_PUBLIC_.
SUPABASE_SERVICE_ROLE_KEY=
